# Create a profile

```python main.py --add-profile test --source-folder source --destination-folder target1 --destination-folder target2```

# Backup a profile to destinations

```python main.py --profile test (--force)```

# Restore a profile from destinations to local folder

```python main.py --profile tes1t --restore (--force)```

